archivesBaseName = "fabric-networking-v0"
version = getSubprojectVersion(project, "0.2.1")

dependencies {
    compile project(path: ':fabric-api-base', configuration: 'dev')
	compile project(path: ':fabric-networking-api-v1', configuration: 'dev')
}
