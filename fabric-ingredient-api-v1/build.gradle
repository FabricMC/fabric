archivesBaseName = "fabric-ingredient-api-v1"
version = getSubprojectVersion(project)

loom {
	accessWidenerPath = file('src/main/resources/fabric-ingredient-api-v1.accesswidener')
}

moduleDependencies(project, [
])
