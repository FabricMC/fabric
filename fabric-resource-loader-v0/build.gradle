archivesBaseName = "fabric-resource-loader-v0"
version = getSubprojectVersion(project)

loom {
	accessWidenerPath = file("src/main/resources/fabric-resource-loader-v0.accesswidener")
}

testDependencies(project, [
	':fabric-lifecycle-events-v1',
	':fabric-api-base'
])

validateMixinNames {
	// Loom bug where it doesnt ignore mixins with multiple targets.
	exclude "**/KeyedResourceReloadListenerMixin.class"
	exclude "**/KeyedResourceReloadListenerClientMixin.class"
}
