archivesBaseName = "fabric-object-builders"
version = getSubprojectVersion(project, "0.1.3")

dependencies {
    compile project(path: ':fabric-api-base', configuration: 'dev')
    compile project(path: ':fabric-tool-attribute-v1', configuration: 'dev')
}
