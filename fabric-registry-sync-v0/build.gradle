archivesBaseName = "fabric-registry-sync-v0"
version = getSubprojectVersion(project, "0.6.1")

moduleDependencies(project, [
		'fabric-api-base',
		'fabric-networking-v0',
		'fabric-registry-api-v1'
])
