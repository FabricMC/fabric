archivesBaseName = "fabric-biomes-v1"
version = getSubprojectVersion(project, "1.1.0")

dependencies {
	testmodCompile project(path: ':fabric-registry-sync-v0', configuration: 'dev')
	testmodCompile project(path: ':fabric-api-base', configuration: 'dev')
}
