archivesBaseName = "fabric-item-groups-v0"
version = getSubprojectVersion(project, "0.2.11")

loom {
	accessWidener = file("src/testmod/resources/test.aw")
}

moduleDependencies(project, [
		'fabric-api-base',
		'fabric-resource-loader-v0'
])
