archivesBaseName = "fabric-rendering-v1"
version = getSubprojectVersion(project)

loom {
	accessWidenerPath = file("src/main/resources/fabric-rendering-v1.accesswidener")
}

moduleDependencies(project, [
		'fabric-api-base'
])
