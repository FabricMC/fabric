archivesBaseName = "forge-particles-v1"
version = getSubprojectVersion(project, "0.1.3")

dependencies {
	compile project(path: ':forge-api-base', configuration: 'dev')
	compile project(path: ':forge-textures-v0', configuration: 'dev')
}
